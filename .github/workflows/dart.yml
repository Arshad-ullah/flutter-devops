name: first-workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: variables
        run: |
          echo "GITHUB_ACTION===> ${{ github.action }}"
          echo "GITHUB_ACTION_PATH ...${{ github.action_path }}"
          echo "GITHUB_EVENT_NAME  ${{ GITHUB.job }}"
          
        
    
