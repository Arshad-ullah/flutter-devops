name: first-workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: variables
        run: |
          echo "GITHUB_ACTION===> ${{ github.action }}"
        
    
